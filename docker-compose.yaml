version: '2'

services:
  dmall-registry-server:
      image: dmall-docker-registry:5000/dmall/order-service:latest
      mem_limit: 512m
      networks:
          - jenkins_dmall-network
      ports:
          - "8084:8084"

networks:
  jenkins_dmall-network:
    external: true